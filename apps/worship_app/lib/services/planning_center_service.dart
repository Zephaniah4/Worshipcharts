class PlanningCenterService {
  Future<String> pullPlanStub(String planId) async {
    // Placeholder for Planning Center API integration.
    await Future<void>.delayed(const Duration(milliseconds: 250));
    return 'Pulled Planning Center plan $planId';
  }
}
